function sum(a, b) {
    return a + b;
}
console.log(sum(1, 1)); //Será executada a soma dos 2 valores (números)
console.log(sum('1', 1)); //Será concatenado a variável 'a'(string), com a variável b (número)
console.log(sum('1', '1')); //Será concatenado as duas variáveis (strings)
